# wyga/roles

Collection of ansible roles for:

 - Ubuntu (22.04, 24.04)
 - Debian (12,13)

in roles folder:
```
git submodule add <GITURL> wyga
git submodule update --recursive --remote
```
or use subtree
