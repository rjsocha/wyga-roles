- name: "Collect Ansible minimal facts ..."
  ansible.builtin.setup:
    gather_subset:
      - 'min'
      - 'network'
      - 'mounts'

- name: "Ensure ansible tmp directory ..."
  ansible.builtin.file:
    path: "{{ ansible_facts.user_dir }}/.ansible/tmp"
    state: directory
    mode: '0700'

- name: "Build platform facts ..."
  wyga_platform:
    devices: "{{ host.platform.boot.devices | default(['auto']) }}"
    distribution: "{{ ansible_distribution | lower }}"
    version: "{{ ansible_distribution_version }}"
    arch: "{{ ansible_architecture }}"
    allow_unsupported: "{{ allow_unsupported | default(false) | bool }}"
    force_distribution: "{{ force_distribution | default(omit) }}"
    force_distribution_version: "{{ force_distribution_version | default(omit) }}"
