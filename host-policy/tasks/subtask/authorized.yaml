---

- include_tasks: "is-public-key-exist.yaml"
  loop: "{{wum_users | dict2items}}"
  loop_control:
    loop_var: pool

- include_tasks: "update-pool.yaml"
  loop: "{{wum_users | dict2items}}"
  loop_control:
    loop_var: pool

- include_tasks: "remove-keys.yaml"
