#jinja2: trim_blocks: True, lstrip_blocks: True
#!/bin/sh
exec /usr/bin/deskflow-server -d "{{ deskflow_debug | upper }}" --name "{{ deskflow_name | upper }}" -a :24800 -f -c /opt/deskflow-control/config
