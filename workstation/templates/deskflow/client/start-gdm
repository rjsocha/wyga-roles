#jinja2: trim_blocks: True, lstrip_blocks: True
#!/bin/sh
exec /usr/bin/deskflow-client -d "{{ deskflow_debug | upper }}" --sync-language --name "{{ deskflow_name | upper }}" -f "{{ deskflow_host }}:24800"
